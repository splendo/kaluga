# Compose Multiplatform libraries only publish for `iosArm64` and `iosSimulatorArm64`. This module
# pulls CMP into commonMain, so we opt out of the legacy `iosX64` (Intel iOS simulator) target to
# avoid Gradle variant resolution failures. Honoured by the kaluga multiplatform plugin.
kaluga.omitIosX64=true
