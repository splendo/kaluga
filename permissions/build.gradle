apply plugin: 'com.android.library'
apply plugin: 'jacoco'
apply plugin: 'maven-publish'
apply plugin: 'org.jetbrains.kotlin.multiplatform'

apply from: "../gradle/component.gradle"

version = gradle.library_version

group = "com.splendo.kaluga"

dependencies {
    implementation "org.jetbrains.kotlinx:kotlinx-coroutines-core:$gradle.kotlinx_corountines_version"
    testImplementation "org.jetbrains.kotlinx:kotlinx-coroutines-debug:$gradle.kotlinx_corountines_version"
    testImplementation 'org.mockito:mockito-core:2.28.2'

    implementation 'androidx.appcompat:appcompat:1.1.0'

    commonTestImplementation "org.jetbrains.kotlinx:kotlinx-coroutines-debug:$gradle.kotlinx_corountines_version"

}

repositories {
    maven { url = "https://dl.bintray.com/pocketbyte/hydra/" }
}

kotlin {
    targets {
        configure([]) {
            tasks.getByName(compilations.main.compileKotlinTaskName).kotlinOptions {
                jvmTarget = "1.8"
            }
        }
    }
    sourceSets {
        getByName("commonMain") {
            dependencies {
                implementation project(":logging", "")
            }
        }
    }
}