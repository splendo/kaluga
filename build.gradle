buildscript {
    ext {
        one_ios_sourceset = true
    }
    repositories {
        google()
        jcenter()

// uncomment for early access / dev releases
//        maven { url 'https://dl.bintray.com/kotlin/kotlin-dev' }
//        maven { url 'https://dl.bintray.com/kotlin/kotlin-eap' }
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.6.0-alpha09'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$gradle.ext.kotlin_version"
        classpath 'com.google.gms:google-services:4.3.2'
    }
}

plugins {
    id 'kotlin-multiplatform' version '1.3.50'
    id 'maven-publish'
}

allprojects {
    repositories {
        google()
        jcenter()

// uncomment for early access / dev releases
//        maven { url 'https://dl.bintray.com/kotlin/kotlin-dev' }
//        maven { url 'https://dl.bintray.com/kotlin/kotlin-eap' }
    }
}

group 'com.splendo.mpp'
version gradle.ext.library_version


publishing {
    publications {
        kotlinMultiplatform {
            artifactId = "components"

            pom {
                name = 'Splendo Kotlin MPP Library'
                description = 'A set of Kotlin Multi Platform Project components'
                url = 'https://splendo.com'
            }
        }
    }
}